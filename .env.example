# 本地：复制为 .env 并填入密钥（与 server 使用的 dotenv 默认文件名一致），或在云平台配置环境变量。
# 仅 Node 服务读取，不会打进浏览器。

# --- Vite 远程开发（可选，写入 .env 后重启 `npm run dev`）---
# 用公网 IP 打开 dev 时出现 `[vite] server connection lost` 并整页刷新：设与地址栏一致的 host，例如：
# DEV_HMR_HOST=43.156.182.61
# DEV_HMR_PORT=3000
# DEV_HMR_CLIENT_PORT=3000
# 若经 HTTPS 反代，需另查 Vite `server.hmr` 的 protocol / clientPort。
# 不需要热更新：`npm run dev:stable`（DISABLE_HMR=true）

GEMINI_API_KEY=

# 远程 dev（浏览器访问 http://公网IP:3000）：视频流式 /api/gemini/stream 建议直连 API，避免 Vite 代理 502
# VITE_API_URL=http://43.156.182.61:8787
# 腾讯云安全组需放行 TCP 8787（来源可为 0.0.0.0/0 或办公网 IP）

# Gemini 出站（新加坡业务机直连 Google；仅 generativelanguage.googleapis.com，PocketBase 仍直连）：
# GEMINI_OUTBOUND_RETRIES=2
# GEMINI_OUTBOUND_RETRY_MS=1000

# 可选：覆盖默认模型 gemini-3-flash-preview
# GEMINI_MODEL=gemini-2.5-flash
# 前端模型下拉（3.1pro / 3.5flash）对应后端真实模型 ID，可按账号可用模型改写：
# GEMINI_MODEL_31PRO=gemini-3.1-pro
# GEMINI_MODEL_35FLASH=gemini-3.5-flash

# 可选：PocketBase（前端必须带 VITE_ 前缀，否则仍默认连 http://127.0.0.1:8090）
# VITE_POCKETBASE_URL=http://127.0.0.1:8090
#
# users 集合增加 Select 字段 userRole：design（设计专员）| placement（投放专员）。
# 注册时写入；旧账号无该字段时前端按 design 处理。create/update 规则需允许用户读写自己的 userRole。
# 若 Admin 里选项是中文，可配置（须与 Select 选项完全一致）：
# VITE_PB_USER_ROLE_DESIGN=设计专员
# VITE_PB_USER_ROLE_PLACEMENT=投放专员

# 使用数据流水 usage_events（与 Node 同机时也可不设，默认 http://127.0.0.1:8090）
# POCKETBASE_URL=http://127.0.0.1:8090
# 服务端写入 gemini.call、like.received 需超级用户/管理员账号（勿提交到仓库）。
# PocketBase 0.23+ 为「超级用户」邮箱密码；旧版为 Admin 账号，服务端会先试 _superusers 再回退 admins。
# POCKETBASE_ADMIN_EMAIL=
# POCKETBASE_ADMIN_PASSWORD=
#
# 在 PocketBase Admin 新建集合 usage_events，建议字段：
# - day (Text, 必填)  例 2026-02-04，上海自然日用前端 formatUsageDayShanghai 与后端一致
# - event (Text, 必填)  取值见 src/lib/usageEvents.ts（如 creative.ideas_generated、gemini.call）
# - user (Relation → users, 必填)  本条流水归属用户
# - source (Text, 可选)
# - ref_collection (Text, 可选)  如 assets、market
# - ref_id (Text, 可选)
# - meta (JSON, 可选)  禁止存 prompt/视频/base64/全文脚本
#
# API Rules 建议：
# - create：@request.auth.id != "" && @request.data.user = @request.auth.id
# - list：仅管理员（或开发阶段先放开再收紧）

# 买量大屏：PocketBase 新建 buying_videos 后由本服务回填 AI 标签（POST /api/buying-videos/ingest）。
# 生产环境必填；与 PocketBase pb_hooks 或外部自动化里配置的 Header X-Ingest-Secret 一致。
# BUYING_VIDEO_INGEST_SECRET=
